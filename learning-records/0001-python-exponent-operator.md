# Python exponent operator corrected

The user correctly noticed that Python code uses `**` for powers, not `^`. This matters because future lessons should distinguish mathematical notation such as `(y_hat - y)^2` from executable NumPy code such as `(y_hat - y) ** 2`.
